# Security Audit Log (Stitch)

- **Project:** Bookkeeping Dashboard  
- **Project ID:** `12603509304854076953`  
- **Screen:** Security Audit Log  
- **Screen ID:** `1ae4cb1bd5c24c1f8c1de7bc5a11a6a2`

## Assets

Export from Stitch and save here (URLs expire):

```bash
curl -L -o security-audit-log.png "<image URL from Stitch export>"
curl -L -o security-audit-log.html "<HTML URL from Stitch export>"
```

## App

UI parity: `frontend/src/routes/audit-log.tsx` (Stitch tokens, **last session** as a compact line in the Activity header, **search** + filters + row count in one toolbar band above the table, single card). No hero/Activity shield icon — title + subtitle only.
